// src/routes/contact/+page.js

// Deshabilita el prerrenderizado para esta página.
// Ya que las páginas con acciones de formulario (`+page.server.js`) no pueden ser prerrenderizadas.
export const prerender = false;